<tr class='<%= prediction.state == 'completed' ? 'success' : 'warning' %>'>
  <td><%= prediction.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
  <td><%= prediction.model %></td>
  <td><%= prediction.test_data %></td>
  <td><%= prediction.state == 'completed' ? '完了' : '実行中' %></td>
</tr>
