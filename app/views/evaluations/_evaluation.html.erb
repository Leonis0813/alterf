<tr class='<%= evaluation.state == 'completed' ? 'success' : 'warning' %>'>
  <td><%= evaluation.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
  <td><%= evaluation.model %></td>
  <td class='td-state'><%= evaluation.state == 'completed' ? '完了' : '実行中' %></td>
</tr>
